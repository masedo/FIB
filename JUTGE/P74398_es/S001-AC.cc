#include <iostream>
#include <cmath>
using namespace std;
int main() {
  int z;
  cin >> z;
  int a=0, b=0, c=0, d=0, e=0, f=0, g=0, h=0, i=0, j=0, k=0, l=0, m=0, n=0, o=0;
  while ((pow(2,a))<=z) {
    ++a;
  }
  while ((pow(3,b))<=z) {
    ++b;
  }
  while ((pow(4,c))<=z) {
    ++c;
  }
  while ((pow(5,d))<=z) {
    ++d;
  }
  while ((pow(6,e))<=z) {
    ++e;
  }
  while ((pow(7,f))<=z) {
    ++f;
  }
  while ((pow(8,g))<=z) {
    ++g;
  }
  while ((pow(9,h))<=z) {
    ++h;
  }
  while ((pow(10,i))<=z) {
    ++i;
  }
  while ((pow(11,j))<=z) {
    ++j;
  }
  while ((pow(12,k))<=z) {
    ++k;
  }
  while ((pow(13,l))<=z) {
    ++l;
  }
  while ((pow(14,m))<=z) {
    ++m;
  }
  while ((pow(15,n))<=z) {
    ++n;
  }
  while ((pow(16,o))<=z) {
    ++o;
  }
  cout << "Base 2: " << a << " cifras." << endl;
  cout << "Base 3: " << b << " cifras." << endl;
  cout << "Base 4: " << c << " cifras." << endl;
  cout << "Base 5: " << d << " cifras." << endl;
  cout << "Base 6: " << e << " cifras." << endl;
  cout << "Base 7: " << f << " cifras." << endl;
  cout << "Base 8: " << g << " cifras." << endl;
  cout << "Base 9: " << h << " cifras." << endl;
  cout << "Base 10: " << i << " cifras." << endl;
  cout << "Base 11: " << j << " cifras." << endl;
  cout << "Base 12: " << k << " cifras." << endl;
  cout << "Base 13: " << l << " cifras." << endl;
  cout << "Base 14: " << m << " cifras." << endl;
  cout << "Base 15: " << n << " cifras." << endl;
  cout << "Base 16: " << o << " cifras." << endl;
}

  